<Window x:Class="WorkTabel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        xmlns:cmd="clr-namespace:WorkTabel.Infrastructure.Commands"
        xmlns:vm="clr-namespace:WorkTabel.ViewModels"
        xmlns:local="clr-namespace:WorkTabel"
        mc:Ignorable="d"
        Title="{Binding Path=Title}" Height="900" Width="1600">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>


    <DockPanel>
        <Menu DockPanel.Dock="Top" VerticalAlignment="Top" Margin="0, 0, 0, 0">

            <MenuItem Header="Файл" ClipToBounds="True">
                <MenuItem Header="Открыть"/>
                <MenuItem Header="Экспорт"/>
                <MenuItem Header="Выход" Command="{Binding CloseApplicationCommand}"/>
            </MenuItem>

            <MenuItem Header="База данных">
                <MenuItem Header="Проверить подключение"/>
                <MenuItem Header="Прервать подкоючение"/>
                <MenuItem Header="Подключиться"/>
            </MenuItem>

            <MenuItem Header="Помощь">
                <MenuItem Header="О программе"/>
            </MenuItem>

        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="Привет"/>
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar Width="120" Height="14" Value="30"/>
            </StatusBarItem>
        </StatusBar>

        <TabControl>
            <TabItem Header="Вкладка 1" HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <GroupBox Grid.Column="0" Header="Отделы">
                        <ListBox  />
                    </GroupBox>
                    <GroupBox Grid.Column="1" Header="Табель" >
                        <DataGrid>

                        </DataGrid>
                    </GroupBox>

                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
